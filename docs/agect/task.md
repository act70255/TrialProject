# 任務清單索引

本專案任務已拆分為三份可獨立使用的 checklist：

- `docs/task-dev.md`：程式開發與技術落地（含 MVP、Phase 2、SQLite 正式化與相容性任務）。
- `docs/task-release.md`：交付、驗收、文件與收尾。
- `docs/task-deploy.md`：Docker 與部署落地（容器、環境變數、健康檢查、部署文件）。

建議執行順序：先 `task-dev`，再 `task-release`；若進入容器化交付，接續 `task-deploy`。
